# Claude Code 装载

```bash
git clone https://github.com/lumitive/lumi-style ~/.claude/skills/lumi-style
```

装载后重启会话或 `/reload-skills`;调用:`/lumi-style <任务>`,或直接说「按 LUMI 风格写…」。
skill 会按需读取 references/ 与 tokens/,不必手动附带。
